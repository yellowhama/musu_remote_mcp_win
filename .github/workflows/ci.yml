name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  docker-build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - name: Build image and run embedded tests
        run: docker build --tag remote-dev-mcp:test .
